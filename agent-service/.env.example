# Optional. If unset, the agent falls back to a deterministic offline
# heuristic instead of calling the LLM (useful for local dev/CI).
ANTHROPIC_API_KEY=

# Model used for risk assessment when ANTHROPIC_API_KEY is set.
SENTINEL_AGENT_MODEL=claude-sonnet-4-6
